#include "pool.hpp"
#include <render/context.hpp>
#include <render/command.hpp>

namespace sword
{
//
//template<typename T, typename B>
//Pool<T, B>::Pool(size_t size) : size{size}, pool(size) {}
//template<typename T, typename B>
//Pool<T, B>::Pool() : size{POOL_DEFAULT_SIZE}, pool(size) {}
//template<typename T, typename B>
//Pool<T, B>::Pool(render::CommandPool&& pool) : size{POOL_DEFAULT_SIZE}, pool(size)
//{
//    gpuCommandPool = std::make_unique<render::CommandPool>(pool);
//}
//
//

}; // namespace sword


